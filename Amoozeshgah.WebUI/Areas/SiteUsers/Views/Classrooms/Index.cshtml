@model Amoozeshgah.ViewModel.ClassroomDto

@using Amoozeshgah.WebUI.HtmlHelpers;
@{
    ViewBag.Title = Model.PageTitle;
}

@Html.LobiPanelWithDataTable(Model, new[] { Html.StringNameFor(x => x.Name), Html.StringNameFor(x => x.Code), Html.StringNameFor(x => x.Capacity) })

@section scripts{
    <script>
        classroomDataTable();
    </script>
}